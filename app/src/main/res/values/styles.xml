<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.my</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="material_drawer_background">@color/primary_dark</item>
        <item name="material_drawer_primary_text">@color/md_white_1000</item>
        <item name="material_drawer_selected">@color/colorAccent</item>
        <item name="material_drawer_secondary_text">@color/md_white_1000</item>
        <item name="material_drawer_selected_text">@color/md_white_1000</item>
    </style>

    <style name="PreferenceThemeOverlay.my"
        parent="PreferenceThemeOverlay">
        <item name="android:background">@color/md_white_1000</item>
    </style>

    <style name="Theme.AppCompat.Dialog.NoActionBar" parent="Base.Theme.AppCompat.Dialog">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
